# scala3-fuel-engine

Pure Scala 3 decision engine for fuel upload row and batch classification.

## Commands

```bash
scala-cli test .
scala-cli run .
```

The public decision functions are:

- `fuel.engine.FuelUploadDecisionEngine.classifyRow`
- `fuel.engine.FuelUploadDecisionEngine.classifyBatch`
